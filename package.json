{
  "name": "chartnotes",
  "version": "0.1.2",
  "description": "Obsidian plugin para gráficos por properties",
  "main": "main.js",
  "scripts": {
    "build": "esbuild main.ts --bundle --minify --outfile=main.js --external:obsidian --format=cjs --platform=node"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "@types/node": "^22.0.0",
    "obsidian": "latest"
  }
}

